version: '3.7'

services:
  db:
    image: mysql:8.0
    restart: always
    env_file:
      - '.env'

     # FOR DEVELOPMENT
    ports:
      - '3306:3306'

     # FOR DEPLOY
#    expose:
#      - 3306
    volumes:
      - ./mysql_data:/var/lib/mysql

  web:
    build: .
    restart: on-failure
    ports:
      - '8003:8000'

  daemon:
    image: bialri/script_daemon
    command: bash -c "python main.py -s 2"
    restart: always
    env_file:
      - .env